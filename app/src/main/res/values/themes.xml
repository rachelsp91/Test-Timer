<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Minimal theme for a full-Compose app.
        Compose handles all UI; this only sets the splash-window background.
        enableEdgeToEdge() in MainActivity handles status/nav bar transparency.
    -->
    <style name="Theme.SmileTimer" parent="android:Theme.Material.NoTitleBar">
        <item name="android:windowBackground">@color/ic_launcher_background</item>
    </style>
</resources>
